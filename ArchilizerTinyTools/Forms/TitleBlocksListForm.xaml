<Window x:Class="ArchilizerTinyTools.Forms.TitleBlocksListForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArchilizerTinyTools.Forms"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="TitleBlocksListForm" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- DataGrid for displaying title blocks -->
        <DataGrid x:Name="TitleBlocksDataGrid" 
                  Grid.Row="1"
                  GridLinesVisibility="None"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False" 
                  CanUserReorderColumns="False" 
                  CanUserResizeColumns="True" 
                  CanUserResizeRows="False" 
                  CanUserSortColumns="True" 
                  IsReadOnly="True"
                  ItemsSource="{Binding}"
                  AlternatingRowBackground="AliceBlue"
                  SelectionMode="Single"
                  EnableColumnVirtualization="False"
                  EnableRowVirtualization="True"
                  MinWidth="300"
                  Margin="10"
                  HeadersVisibility="Column">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding FamilyName}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding TitleBlockName}"/>
            </DataGrid.Columns>

        </DataGrid>

        <!-- OK Button -->
        <Button Grid.Row="2" 
                x:Name="btn_OK" 
                Content="Ok" 
                Width="80"
                Height="30"
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Click="btn_OK_Click"/>

    </Grid>
</Window>
